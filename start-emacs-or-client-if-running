if pidof -x "emacs" ; then # if emacs already running
    emacsclient -n $@ # $@ is all arguments passed, adds to running emacs sesh
else
    \emacs -nw -l ~/.emacs.d/.emacs -nw $@ # else start it up
fi
